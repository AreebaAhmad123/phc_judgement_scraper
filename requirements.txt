# Pinned to exact versions per brief Section 12 ("pinned to specific
# versions", not ranges). Resolved and installed together into a clean
# venv on 2026-07-14; tests/test_citation_parser.py, test_naming.py,
# test_parser.py, and test_storage_and_dedup.py pass against this set.
# sentence-transformers could not be installed in the verification
# sandbox (disk space), but embeddings.py only uses the long-stable
# SentenceTransformer(name).encode(...) surface, so the pin below should
# hold; re-run `pip freeze` after your own first successful `pytest -v`
# and adjust this one line if you hit an incompatibility.
requests==2.34.2
beautifulsoup4==4.15.0
urllib3==2.7.0
APScheduler==3.11.3
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-dotenv==1.2.2
weaviate-client==4.22.0
sentence-transformers==5.6.0
groq==1.5.0
pymupdf4llm==1.28.0
PyMuPDF==1.28.0
google-api-python-client==2.198.0
google-auth==2.56.0
google-auth-oauthlib==1.4.0
numpy==2.2.0
pytest==9.1.1
boto3==1.43.47
